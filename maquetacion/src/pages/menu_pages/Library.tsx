import { IonContent, IonText } from "@ionic/react";

const library = () => {
    return (
        <IonContent>
            <IonText>Libreria</IonText>
        </IonContent>
    );
}

export default library;