import { IonContent, IonText } from "@ionic/react";

const Settings = () => {
    return (
        <IonContent>
            <IonText>Settings</IonText>
        </IonContent>
    );
}

export default Settings;