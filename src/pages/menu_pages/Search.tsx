import { IonContent, IonText } from "@ionic/react";

const Search = () => {
    return (
        <IonContent>
            <IonText>Search</IonText>
        </IonContent>
    );
}

export default Search;